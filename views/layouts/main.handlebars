<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootswatch/4.1.3/sketchy/bootstrap.min.css" />
    <link rel="stylesheet" href="/stylesheets/style.css"/>
</head>

<header>
    <nav class="navbar navbar-dark bg-info m-1">
        <ul class="nav">
            <li class="nav-item"><a href="/" class="nav-link">Home</a></li>
            {{#if loggedIn}}
            <li class="nav-item"><a href="/dashboard" class="nav-link">Dashboard</a></li>
            <button type="button" class="btn bg-info " id="logout">Logout</button>
            {{#if userName}}
            <li class="navbar-brand">{{userName}}</li>
            {{/if}}
            {{!-- <li class="nav-item" id="logout" class="nav-link">Logout</a></li> --}}
            {{else}}
            <li class="nav-item"><a href="/login" class="nav-link">Login</a></li>
            {{/if}}
        </ul>

    </nav>
</header>

<body>
    <main>
        {{{ body }}}
    </main>
    </div>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>