<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootswatch/4.1.3/sketchy/bootstrap.min.css" />
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css"
        integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous" />

    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<header>
    <nav class="navbar navbar-dark bg-info m-1">
        <ul class="nav">
            <li class="nav-item"><a href="/" class="nav-link">Home</a></li>
            {{#if loggedIn}}
            <li class="nav-item"><a href="/dashboard" class="nav-link">Dashboard</a></li>
            <button type="button" class="btn bg-info font-weight-bold" id="logout">Logout</button>
            {{else}}
            <li class="nav-item"><a href="/login" class="nav-link">Login</a></li>
            {{/if}}
        </ul>
        {{#if username}}
        <ul class="navbar-nav ml-auto">
            <li class="nav-item navbar-brand bg-success px-2">
                {{username}}
            </li>
        </ul>
        {{/if}}
    </nav>
</header>

<body>
    <main>
        {{{ body }}}
    </main>
    </div>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>