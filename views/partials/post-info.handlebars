<div class="card mt-3 mx-3">
    <div class="card-header bg-info ">
        {{title}} : Posted by {{user.username}} on {{ formatDate created_at }}
    </div>
    <div class="card-body">
        <p>{{post_text}}</p>
    </div>
</div>
