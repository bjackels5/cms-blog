<div class="card mt-3 mx-3">
    <div class="card-header bg-info py-1">
        {{title}} <span class="float-right">Posted by {{user.username}} on {{ formatDate created_at }}</span>
    </div>
    <div class="card-body pt-1 mb-0 pb-0">
        <p class="card-text">{{post_text}}</p>
    </div>
</div>
